import { FormcontrolUiDirective } from './formcontrol-ui.directive';

describe('FormcontrolUiDirective', () => {
  it('should create an instance', () => {
    const directive = new FormcontrolUiDirective();
    expect(directive).toBeTruthy();
  });
});
